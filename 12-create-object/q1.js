const info = {
  name: 'Artem',
  surname: 'Tykhonenko',
  favoriteNumber: 7,
};

const person = `My name is ${info.name} ${info.surname} and my favorite number is ${info.favoriteNumber}`;

console.log(person);
